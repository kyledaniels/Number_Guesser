let humanScore = 0;
let computerScore = 0;
let currentRoundNumber = 1;

// Write your code below:

//Function that will generate number at start of game:

const generateTarget = ()=>{
   return Math.floor(Math.random()*10);
}